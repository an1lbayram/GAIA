{
  "name": "gaia-quiz-platform",
  "version": "1.0.0",
  "description": "GAIA - İnteraktif Sınav Hazırlık ve Analiz Platformu",
  "private": true,
  "scripts": {
    "dev": "npm --prefix quiz-app run dev",
    "start": "npm --prefix quiz-app run dev",
    "build": "npm --prefix quiz-app run build",
    "preview": "npm --prefix quiz-app run preview",
    "lint": "npm --prefix quiz-app run lint"
  }
}
